<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ksu.nafea">


    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.CourseManagementActivity"></activity>
        <activity
            android:name=".ui.MajorActivity"
            android:parentActivityName=".ui.LoginActivity" />
        <activity
            android:name=".ui.DepartmentPlanActivity"
            android:parentActivityName=".ui.MajorActivity" />
        <activity android:name=".ui.TestActiv" />
        <activity
            android:name=".ui.BrowseActivity"
            android:parentActivityName=".ui.RegisterActivity" />
        <activity
            android:name=".ui.RegisterActivity"
            android:parentActivityName=".ui.LoginActivity" />
        <activity android:name=".ui.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>